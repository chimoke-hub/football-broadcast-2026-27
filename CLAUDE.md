# football-broadcast-2026-27

2026-27シーズンの欧州5大リーグ＆トッテナムの放送チャンネル・試合日程をまとめた静的サイト（ビルドツールなし、素のHTML/CSSのみ）。GitHub Pagesで配信。

- `index.html` — 欧州5大リーグ＆日本サッカーの放送チャンネル一覧
- `tottenham.html` — トッテナム全試合日程・放送チャンネル
- `tottenham.ics` — トッテナムの試合日程カレンダー（購読用）
- `analysis/` — トッテナム試合の詳細分析ページ（本ドキュメントの対象）

デザインは各HTMLファイルに`<style>`を直書きする方式（共通CSSファイルなし）。ダークモード対応はCSS変数＋`prefers-color-scheme`＋`data-theme`属性の3段構成（既存ファイルからコピーする）。

## 試合分析システム（analysis/）

**目的**：トッテナムの毎試合終了後に、スタッツ・戦術分析をメインにした詳細な振り返りページを作る。

**運用方針**：完全自動化はせず、ユーザーが試合ごとに「この試合を分析して」と依頼したときに、以下の手順で生成する（Routine化は将来の拡張候補）。

### データソース：API-Football

- 無料枠（1日100リクエスト）で開始。トッテナム単体なら1試合あたり数リクエストで足りる規模。
- ベースURL: `https://v3.football.api-sports.io`
- 認証ヘッダー: `x-apisports-key: <APIキー>`
- **APIキーは絶対にリポジトリにコミットしない。** ユーザーがチャットで渡すか、セッションの環境変数として渡してもらい、その場でAPI呼び出しに使うだけにする（ファイルに書き出さない）。
- **無料枠には現在シーズンのデータへのアクセス制限がある（2026年9月に実機確認済み）。** `season=2026`を指定して`/fixtures`を叩くと `"Free plans do not have access to this season, try from 2022 to 2024."` というエラーが返る。つまり無料枠は2022〜2024シーズンの過去データのみが対象で、2026-27シーズン（進行中）の試合分析には使えない。どの有料プランから現在シーズンが解放されるかは`https://www.api-football.com/pricing`で要確認（本ドキュメント作成時点ではネットワークポリシーの都合で未確認）。
- `last=N`パラメータ（直近N試合取得）も無料枠では使用不可（`"Free plans do not have access to the Last parameter."`）。フィクスチャ特定は`team`+`season`+`date`（または`league`+`round`）で行う想定だが、上記の制限により現状は有料プラン契約後でないと本番運用できない。

### 手順

1. **チームIDの解決**（初回のみ、または不明な場合）
   `GET /teams?search=Tottenham` でトッテナムの内部team IDを取得する。IDを推測でハードコードしない。一度確認できたら、このファイルに追記して以後使い回してよい。
   - トッテナムのteam ID: `47`（2026年9月確認済み。トップチーム。U21/U23/女子チーム等の同名チームと混同しないこと）

2. **対象試合の特定**
   `GET /fixtures?team={team_id}&season=2026&date={YYYY-MM-DD}` またはユーザーが指定した対戦カードで該当fixture IDを特定する。ステータスが試合終了（`FT`）であることを確認する。

3. **データ取得**（fixture IDが分かったら）
   - `GET /fixtures?id={fixture_id}` — スコア、日時、スタジアム、審判、大会情報
   - `GET /fixtures/statistics?fixture={fixture_id}` — 両チームのスタッツ（支配率、シュート数・枠内数、パス数・成功率、コーナー、ファウル、オフサイド、カード枚数、GKセーブ数など）
   - `GET /fixtures/lineups?fixture={fixture_id}` — フォーメーション・スタメン・控え・監督
   - `GET /fixtures/events?fixture={fixture_id}` — 得点・カード・交代の時系列イベント
   - `GET /fixtures/players?fixture={fixture_id}` —（任意）選手個人スタッツ・採点。総括で触れたい場合に使う。

4. **戦術分析の執筆**
   取得したスタッツ・フォーメーション・交代タイミングを踏まえて、以下を文章化する（単なる数値の言い換えではなく、試合展開の解釈を書く）：
   - 両チームの基本布陣とプレスタイル（ハイプレスか撤退守備か、ボール保持志向か等）
   - 試合の流れが変わったポイント（先制点、退場、采配など）
   - 交代采配の意図と効果
   - 次節に向けた注目点
   - **同じ統計（xGやシュート数など）を複数のセクションで言い換えながら繰り返さない。** 各数値は原則1箇所でだけ具体的に示す
   - **「〜と言える」「〜だろう」「〜と見られる」のような紋切り型の語尾を段落ごとに機械的に繰り返さない。** 言い切れることは言い切る
   - 見出しは全部を同じ構文（「〜が生んだ〜」「〜に浮き彫りになった〜」等）で揃えず、実際の記事らしく自然に変える

5. **ページ生成**
   - `analysis/_template.html`をコピーして新しいファイルを作る。ファイル名: `analysis/YYYY-MM-DD-{home-slug}-{away-slug}.html`（スラッグは英小文字・ハイフン区切り。例: `analysis/2026-08-30-tottenham-newcastle.html`）
   - テンプレート内のコメント（ファイル冒頭）に「差し替えが必要な箇所」「変更不要な箇所」が明記されているので、それに従って中身を対象の試合のデータに丸ごと差し替える
   - `analysis/index.html`の`.match-list`内、先頭（最新試合が上）に`match-card`を追加する。1件でも追加したら`.empty-state`ブロックは削除する
   - `tottenham.html`の該当行の「カード」列に「 · `<a href="analysis/xxx.html">分析</a>`」を追記する（該当試合が消化済みになったタイミング）

### ページテンプレート

`analysis/_template.html`が完成形のリファレンス実装（2026-27シーズンの試合ではなく、無料枠の過去データで作った動くサンプル。ファイル冒頭のコメントに差し替え手順あり）。新しい分析ページはこれをコピーして作る。構成要素は以下の通り、上から順に並べる：

1. **ヘッダー（スコアボード）** — 両チームのエンブレム（`https://media.api-sports.io/football/teams/{team_id}.png`）とスコアを大きく中央表示、大会・節・日時・会場・主審
2. **クイックナビ** — ページ内の各カードへ飛べるアンカーリンクをスティッキー表示（試合結果／スタッツ比較／フォーメーション／選手スタッツ／レーダー比較／注目選手／戦術分析／タイムライン／総括）
3. **カード「試合結果」** — 導入のリード文＋両チームの得点者・アシスト、警告/退場のサマリー
4. **カード「スタッツ比較」** — 支配率・シュート数・枠内シュート・xG・パス成功率・コーナー・ファウル・GKセーブ等を横棒（`.stat-row`/`.stat-bar`/`.stat-bar-fill`）で両チーム比較
5. **カード「フォーメーション・スタメン」** — `fixtures/lineups`の`grid`座標を使い、実際のポジションに基づいたピッチ図（`.pitch`/`.pitch-row`/`.pitch-player`）で両チームを表示。`.pitch`の`min-height`とテーブルの`<colgroup>`は両チームで完全に同じ値を使い、サイズが揃うようにする（内容量で変わってはいけない）
6. **カード「選手スタッツ」** — 出場した全選手のスタッツ表（選手／Pos／分／G／A／シュート(枠内)／パス(成功)／KP／デュエル(勝)／ドリブル(成功)／警告／評点）を両チーム分。各行に`class="player-row" data-player="{id}"`を付け、クリックでレーダー比較に追加できるようにする
7. **カード「レーダー比較」** — 選手スタッツの行をクリックすると最大4人まで同時にレーダーチャートへ追加されるインタラクティブUI（`<script>`内のPLAYERS/AXESデータとrender/toggle関数、色は`--radar-1`〜`--radar-4`のCSS変数）。固定の「MVP2人比較」ではなく、必ずこのクリック式にする
8. **カード「注目選手」** — 2〜4名を見出し＋段落で深掘り
9. **カード「戦術分析」** — 見出し（h4）で区切った複数段落の読み物
10. **カード「タイムライン」** — 得点・警告・交代を時系列で。得点はサッカーボールのアイコン、交代は「退場（グレーの下矢印）→出場（グリーンの上矢印）」のアイコンを使う（`.timeline`/`.ev-goal`/`.ev-card`/`.ev-sub`/`.sub-tag`）
11. **カード「総括」** — 複数段落
12. **フッター** — 非公式の免責＋リポジトリリンク

**アイコン**：見出しやタイムラインの視覚要素には[icooon-mono.com](https://icooon-mono.com/)（商用利用可・帰属表示不要のフリー素材）から取得したモノクロSVGを使う。`<body>`直後に`<svg style="display:none">`のスプライトとして埋め込み済み（`_template.html`からそのままコピーする）。絵文字は使わない。

**写真について**：試合中の実写真（選手のプレー中の写真等）は報道写真会社の著作権で保護されているため、無断で使用しない。エンブレム・スコアボードのみで構成し、写真は入れない方針。
